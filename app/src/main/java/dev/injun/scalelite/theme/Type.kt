package dev.injun.scalelite.theme

import androidx.compose.material3.Typography

/**
 * The default Material scale. The one style the project template overrode was set to
 * the values the default already has, so there is nothing to say here.
 */
val Typography = Typography()
